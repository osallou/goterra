redis:
        host: "got-redis"
        port: 6379
        prefix: "goterra"

mongo:
        url: "mongodb://got-mongo:27017"
        db: "goterra"


# url to traefik
url: "http://localhost:8081"

fernet:
        - "0123456789"

web:
        listen: "0.0.0.0"
        port: 8000

deploy:
        # path where terraform scripts are written
        path: "/var/lib/got"
